# Thin client runtime deps (cross-platform). No AI/ffmpeg/moviepy.
fastapi
starlette>=0.46,<1.0  # Starlette 1.x breaks FastAPI route registration; pin to 0.x
uvicorn
httpx
jinja2
python-multipart
pywebview
# macOS webview backend (Cocoa/WebKit)
pyobjc-framework-Cocoa; sys_platform == "darwin"
pyobjc-framework-WebKit; sys_platform == "darwin"
